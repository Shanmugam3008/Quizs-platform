<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard - Quiz Platform</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .dashboard-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px 15px;
        }
        .welcome-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 40px;
            border-radius: 15px;
            margin-bottom: 40px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }
        .stat-card {
            background: white;
            border-radius: 15px;
            padding: 30px 25px;
            text-align: center;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border: none;
            height: 100%;
            margin-bottom: 25px;
        }
        .stat-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.15);
        }
        .stat-icon {
            font-size: 3rem;
            margin-bottom: 20px;
            opacity: 0.9;
        }
        .stat-number {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 10px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }
        .quiz-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            margin-bottom: 30px;
            overflow: hidden;
        }
        .quiz-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }
        .section-title {
            font-size: 1.6rem;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 4px solid #667eea;
        }
        .btn-quiz {
            background: linear-gradient(135deg, #667eea, #764ba2);
            border: none;
            padding: 12px 30px;
            border-radius: 30px;
            color: white;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }
        .btn-quiz:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }
        .quiz-item {
            background: white;
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 20px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.08);
            border: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }
        .quiz-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
        }
        .welcome-buttons .btn {
            padding: 10px 25px;
            font-weight: 600;
            border-radius: 25px;
            margin: 0 5px;
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <div class="dashboard-container">
        <!-- Welcome Section -->
        <div class="welcome-section text-center">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <h1 class="display-4 fw-bold mb-3">Welcome back, <%= user.username %>! 👋</h1>
                    <p class="lead mb-4 fs-5">Ready to test your knowledge? Explore available quizzes below and continue your learning journey.</p>
                    <div class="welcome-buttons">
                        <a href="/user/profile" class="btn btn-light btn-lg">
                            <i class="fas fa-user me-2"></i>My Profile
                        </a>
                        <a href="/user/results" class="btn btn-outline-light btn-lg">
                            <i class="fas fa-chart-bar me-2"></i>View Results
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <!-- Quick Stats -->
        <div class="row mb-5">
            <div class="col-md-3">
                <div class="stat-card">
                    <div class="stat-icon text-primary">
                        <i class="fas fa-question-circle"></i>
                    </div>
                    <div class="stat-number"><%= quizzes.length %></div>
                    <div class="text-muted fw-semibold fs-6">Available Quizzes</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stat-card">
                    <div class="stat-icon text-success">
                        <i class="fas fa-chart-bar"></i>
                    </div>
                    <div class="stat-number"><%= results.length %></div>
                    <div class="text-muted fw-semibold fs-6">Quizzes Taken</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stat-card">
                    <div class="stat-icon text-warning">
                        <i class="fas fa-trophy"></i>
                    </div>
                    <div class="stat-number">
                        <% 
                            let totalScore = 0;
                            let totalQuestions = 0;
                            results.forEach(result => {
                                totalScore += result.score;
                                totalQuestions += result.total_questions;
                            });
                            const avgScore = results.length > 0 ? ((totalScore / totalQuestions) * 100).toFixed(0) : 0;
                        %>
                        <%= avgScore %>%
                    </div>
                    <div class="text-muted fw-semibold fs-6">Average Score</div>
                </div>
            </div>
            <div class="col-md-3">
                <div class="stat-card">
                    <div class="stat-icon text-info">
                        <i class="fas fa-star"></i>
                    </div>
                    <div class="stat-number">
                        <%
                            const passedQuizzes = results.filter(result => {
                                return (result.score / result.total_questions) >= 0.6;
                            }).length;
                        %>
                        <%= passedQuizzes %>
                    </div>
                    <div class="text-muted fw-semibold fs-6">Quizzes Passed</div>
                </div>
            </div>
        </div>

        <!-- Available Quizzes Section -->
        <div class="quiz-card">
            <div class="card-header bg-white border-bottom-0 py-4">
                <div class="row align-items-center">
                    <div class="col-md-8">
                        <h3 class="section-title mb-0">
                            <i class="fas fa-list-alt me-3"></i>Available Quizzes
                        </h3>
                    </div>
                    <div class="col-md-4 text-end">
                        <span class="badge bg-primary fs-6 p-2">
                            Total: <%= quizzes.length %> Quizzes
                        </span>
                    </div>
                </div>
            </div>
            <div class="card-body p-4">
                <% if (quizzes.length > 0) { %>
                    <div class="row g-4">
                        <% quizzes.forEach(quiz => { %>
                            <div class="col-lg-4 col-md-6">
                                <div class="quiz-item">
                                    <div class="text-center mb-3">
                                        <i class="fas fa-code fa-3x text-primary mb-3"></i>
                                        <h4 class="fw-bold text-dark"><%= quiz.title %></h4>
                                    </div>
                                    <p class="text-muted text-center mb-4">
                                        <%= quiz.description || 'Test your knowledge with this comprehensive quiz' %>
                                    </p>
                                    <div class="text-center">
                                        <a href="/user/take-quiz/<%= quiz.id %>" class="btn btn-quiz">
                                            <i class="fas fa-play-circle me-2"></i>Start Quiz
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="text-center py-5">
                        <i class="fas fa-inbox fa-5x text-muted mb-4"></i>
                        <h3 class="text-muted mb-3">No quizzes available at the moment</h3>
                        <p class="text-muted fs-5">We're working on adding new quizzes. Please check back soon!</p>
                    </div>
                <% } %>
            </div>
        </div>

        <!-- Quick Stats Info -->
        <div class="row mt-5">
            <div class="col-12">
                <div class="quiz-card">
                    <div class="card-body text-center p-5">
                        <h3 class="mb-4">Keep Learning and Improving! 🚀</h3>
                        <p class="lead mb-4">
                            Regular practice is key to mastering any skill. Take quizzes regularly to track your progress 
                            and identify areas for improvement.
                        </p>
                        <div class="row">
                            <div class="col-md-4 mb-3">
                                <i class="fas fa-brain fa-2x text-primary mb-2"></i>
                                <h5>Enhance Knowledge</h5>
                                <p class="text-muted">Learn new concepts and reinforce existing knowledge</p>
                            </div>
                            <div class="col-md-4 mb-3">
                                <i class="fas fa-chart-line fa-2x text-success mb-2"></i>
                                <h5>Track Progress</h5>
                                <p class="text-muted">Monitor your improvement over time</p>
                            </div>
                            <div class="col-md-4 mb-3">
                                <i class="fas fa-award fa-2x text-warning mb-2"></i>
                                <h5>Earn Achievements</h5>
                                <p class="text-muted">Unlock achievements as you progress</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
   
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <script src="/js/script.js"></script>
</body>
</html>