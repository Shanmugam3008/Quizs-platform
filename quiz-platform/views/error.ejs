<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %>
    <style>
        body {
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .error-container {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 1rem;
        }
        .error-card {
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.1);
            border: none;
            max-width: 600px;
            width: 100%;
            text-align: center;
        }
        .error-header {
            background: linear-gradient(45deg, #ff6b6b, #ee5a52);
            color: white;
            padding: 30px;
        }
        .error-body {
            padding: 30px;
            background-color: #fff;
        }
        .error-icon {
            font-size: 5rem;
            color: #ff6b6b;
            margin-bottom: 20px;
        }
        .error-content {
            margin-bottom: 25px;
        }
        .btn-primary {
            background: linear-gradient(45deg, #4a6bff, #6a11cb);
            border: none;
            border-radius: 8px;
            padding: 12px 25px;
            font-weight: 600;
            transition: all 0.3s;
            color: white;
        }
        .btn-primary:hover {
            background: linear-gradient(45deg, #405bd8, #5a0fb8);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(74, 107, 255, 0.3);
        }
        .error-details {
            background-color: #f8f9fa;
            border-radius: 10px;
            padding: 15px;
            margin-top: 20px;
            border-left: 4px solid #ff6b6b;
        }
        .error-actions {
            margin-top: 25px;
        }
        .error-actions a {
            margin: 0 10px;
        }
        .pulse {
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
        .error-code {
            font-size: 0.9rem;
            color: #6c757d;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="error-container">
        <div class="error-card">
            <div class="error-header">
                <h2><i class="fas fa-exclamation-triangle me-2"></i>Oops! Something went wrong</h2>
            </div>
            
            <div class="error-body">
                <div class="error-icon pulse">
                    <i class="fas fa-exclamation-circle"></i>
                </div>
                
                <div class="error-content">
                    <h4 class="text-danger">Error</h4>
                    <p class="lead"><%= message %></p>
                </div>
                
                <div class="error-details">
                    <p>We're sorry for the inconvenience. Please try one of the following:</p>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-arrow-right me-2 text-primary"></i>Return to the homepage</li>
                        <li><i class="fas fa-arrow-right me-2 text-primary"></i>Check your connection and try again</li>
                        <li><i class="fas fa-arrow-right me-2 text-primary"></i>Contact support if the problem persists</li>
                    </ul>
                </div>
                
                <div class="error-actions">
                    <a href="/" class="btn btn-primary">
                        <i class="fas fa-home me-2"></i>Go Home
                    </a>
                    <a href="javascript:history.back()" class="btn btn-outline-primary">
                        <i class="fas fa-arrow-left me-2"></i>Go Back
                    </a>
                </div>
                
                <div class="error-code">
                    Error code: <%= typeof status !== 'undefined' ? status : 'Unknown' %>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Font Awesome for icons -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    
    <!-- Bootstrap JS -->
    
    <%- include('partials/footer') %>
</body>
</html>
