<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .register-container {
            max-width: 500px;
            width: 100%;
        }
        .register-card {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            border: none;
        }
        .register-header {
            background: linear-gradient(45deg, #4a6bff, #6a11cb);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .register-body {
            padding: 30px;
            background-color: #fff;
        }
        .form-control {
            border-radius: 8px;
            padding: 12px 16px;
            border: 2px solid #e2e8f0;
            transition: all 0.3s;
        }
        .form-control:focus {
            border-color: #4a6bff;
            box-shadow: 0 0 0 3px rgba(74, 107, 255, 0.2);
        }
        .form-label {
            font-weight: 600;
            color: #2d3748;
            margin-bottom: 8px;
        }
        .btn-register {
            background: linear-gradient(45deg, #4a6bff, #6a11cb);
            border: none;
            border-radius: 8px;
            padding: 12px;
            font-weight: 600;
            transition: all 0.3s;
            color: white;
        }
        .btn-register:hover {
            background: linear-gradient(45deg, #405bd8, #5a0fb8);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(74, 107, 255, 0.3);
        }
        .alert {
            border-radius: 10px;
            padding: 15px 20px;
        }
        .register-footer {
            text-align: center;
            margin-top: 20px;
            color: #718096;
        }
        .register-footer a {
            color: #4a6bff;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s;
        }
        .register-footer a:hover {
            color: #405bd8;
        }
        .icon-wrapper {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 70px;
            height: 70px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            margin-bottom: 15px;
        }
        .password-requirements {
            background-color: #f8f9fa;
            border-radius: 8px;
            padding: 15px;
            margin-top: 20px;
            font-size: 0.85rem;
        }
        .password-requirements ul {
            margin-bottom: 0;
            padding-left: 20px;
        }
        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
        }
        .form-text {
            font-size: 0.85rem;
            color: #6c757d;
        }
    </style>
</head>
<body>
    <div class="register-container">
        <div class="register-card">
            <div class="register-header">
                <div class="icon-wrapper">
                    <svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" fill="currentColor" viewBox="0 0 16 16">
                        <path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6zm2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0zm4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4zm-1-.004c-.001-.246-.154-.986-.832-1.664C11.516 10.68 10.289 10 8 10c-2.29 0-3.516.68-4.168 1.332-.678.678-.83 1.418-.832 1.664h10z"/>
                    </svg>
                </div>
                <h3>Create Account</h3>
                <p class="mb-0">Join us to start creating and taking quizzes</p>
            </div>
            
            <div class="register-body">
                <% if (errors.length) { %>
                    <div class="alert alert-danger">
                        <strong>Please fix the following errors:</strong>
                        <% errors.forEach(error => { %>
                            <div class="mb-0 mt-2"><i class="bi bi-exclamation-circle-fill"></i> <%= error.msg %></div>
                        <% }) %>
                    </div>
                <% } %>
                
                <form method="POST" action="/register">
                    <div class="form-group">
                        <label for="username" class="form-label">Username</label>
                        <input type="text" class="form-control" id="username" name="username" 
                            value="<%= formData.username || '' %>" placeholder="Choose a username" required>
                        <div class="form-text">Must be at least 3 characters long</div>
                    </div>
                    
                    <div class="form-group">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" 
                            value="<%= formData.email || '' %>" placeholder="Enter your email" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" 
                            placeholder="Create a password" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="confirmPassword" class="form-label">Confirm Password</label>
                        <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" 
                            placeholder="Confirm your password" required>
                    </div>
                    
                    <div class="password-requirements">
                        <strong>Password Requirements:</strong>
                        <ul>
                            <li>At least 6 characters long</li>
                            <li>Should match the confirmation field</li>
                        </ul>
                    </div>
                    
                    <button type="submit" class="btn btn-register w-100">Create Account</button>
                </form>
                
                <div class="register-footer">
                    <p>Already have an account? <a href="/login">Sign in here</a></p>
                </div>
            </div>
        </div>
    </div>
    
    <%- include('../partials/footer') %>
</body>
</html>
